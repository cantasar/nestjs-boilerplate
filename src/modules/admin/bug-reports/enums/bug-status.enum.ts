/**
 * Placeholder bug-report lifecycle states shipped with the boilerplate. Replace
 * / extend the members with your application's real triage workflow. The values
 * are persisted as plain strings (see `bug_reports.status`).
 */
export enum BugStatus {
  OPEN = 'open',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  WONT_FIX = 'wont_fix',
}
