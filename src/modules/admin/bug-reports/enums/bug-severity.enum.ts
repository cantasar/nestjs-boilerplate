/**
 * Placeholder bug-report severity levels shipped with the boilerplate. Replace /
 * extend the members with your application's real severity ladder. The values
 * are persisted as plain strings (see `bug_reports.severity`).
 */
export enum BugSeverity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
