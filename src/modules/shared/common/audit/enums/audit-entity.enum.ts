/**
 * Placeholder audited entity names shipped with the boilerplate. Replace the
 * members with your application's real domain entities (one per auditable
 * resource). The values are persisted as plain strings.
 */
export enum AuditEntity {
  USER = 'user',
  RESOURCE = 'resource',
}
