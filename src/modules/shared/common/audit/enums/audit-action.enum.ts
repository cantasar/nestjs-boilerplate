/**
 * Placeholder audit action verbs shipped with the boilerplate. Replace the
 * members with your application's real mutation actions (e.g. ARCHIVE,
 * LOGIN_SUCCESS, STATUS_CHANGE). The values are persisted as plain strings.
 */
export enum AuditAction {
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
}
